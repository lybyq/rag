# Web 产品与全链路验收：练习

1. 在 Playwright 中让第二个分片第一次返回 503，验证只重传失败分片且总进度不倒退。
2. 让 SSE 连续失败并让轮询返回 PARTIAL，观察消息视觉、警告和取消按钮。
3. 在答案生成后撤销空间权限，再点击引用，确认抽屉不显示旧正文。
4. 修改 `vue-element-plus-x` mock 的事件名，观察 Adapter 测试如何把 breaking change 限定在边界。
5. 返回缺少 `traceId` 的任务响应，解释 Zod 错误应在哪一层转成用户可行动提示。
6. 只用键盘完成上传、取消问答和打开引用，记录不可达控件并修复。
