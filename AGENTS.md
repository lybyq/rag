# RAG 项目协作约定

本文件约束本仓库后续由开发者或 Codex 完成的所有实现。需求真相位于 `docs/requirements/`。

## 1. 实施顺序

1. 开始模块前先读取 `docs/requirements/README.md` 和对应模块需求。
2. 一个需求只有代码、测试、可观测性、文档和验收同时完成后才能勾选。
3. 不得跳过当前模块的验收门禁直接实现依赖它的模块。
4. 每次变更必须在测试名称或 JSDoc 中标注关联需求编号，例如 `AUTH-003`。
5. 遇到需求与实现冲突时先更新 ADR 和需求基线，不允许让代码成为未记录的例外。

## 2. 后端代码规范

- 使用 TypeScript 严格模式、NestJS、Zod 运行时校验和显式依赖注入。
- Domain 不依赖 NestJS、Prisma、Redis、Milvus、MinIO 或模型 SDK。
- Controller/Processor 只负责输入映射、调用 Use Case 和输出映射。
- 所有外部系统都通过 Port 接口访问，不在业务代码中读取供应商特有配置。
- 所有远程调用必须支持 Deadline、单次超时、`AbortSignal`、错误分类和 Trace。
- 权限默认拒绝；用户输入不得直接构造 SQL、对象路径或 Milvus Filter。

## 3. 中文注释与教学要求

- 每个源文件顶部用中文 JSDoc 解释：文件职责、所在链路、为什么需要它，以及不应承担什么职责。
- 每个导出的类、函数、接口、类型和重要字段必须有中文 JSDoc。
- 状态机、事务、幂等、重试、权限过滤、算法和模型调用必须有详细中文行内说明。
- 不给 import、括号、简单赋值等自明语句堆砌重复注释；逐行级讲解写入模块 `code-walkthrough.md`，按代码执行顺序解释。
- 每个模块必须包含原理、执行链路、调试方法、常见故障、取舍和面试追问。
- 示例不得隐藏关键错误处理，不使用“生产中请自行补充”代替真正实现。

## 4. 前端代码规范

- 使用 Vue 3、Composition API、`<script setup lang="ts">`、Vue Router 和 Pinia。
- 基础管理组件使用 Element Plus；AI 会话组件使用 Element Plus X，但必须经 `components/ai-adapter` 封装，业务页面不得散落第三方专有 API。
- Route View 只负责组合，不承载完整业务逻辑；状态和副作用进入 feature composable。
- 上传进度必须来自后端真实任务事件；不得用定时器伪造进度。
- 所有状态都要有加载、空数据、失败、重试、无权限和取消后的界面。

## 5. 测试与安全

- 领域规则先写失败测试，再实现代码。
- 每个外部 Port 都要覆盖正常、超时、取消、Schema 错误、429/5xx、版本不匹配和部分失败。
- 权限、引用、索引发布、状态迁移与幂等测试属于强制门禁。
- 测试数据必须公开、合成或经批准脱敏；日志不得记录密钥、预签名 URL、完整问题或敏感证据。
